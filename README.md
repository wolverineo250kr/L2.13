# cut — простая утилита для выборки полей из строк (аналог GNU cut)

Утилита считывает данные из STDIN, разбивает строки по разделителю и выводит указанные поля.

Поддерживает флаги:

- **-f** — список полей (номера или диапазоны)
- **-d** — разделитель (по умолчанию `\t`)
- **-s** — выводить только строки, содержащие разделитель
- **--output-delimiter** — установить разделитель вывода

---

## 🔧 Сборка
```
go build -o cut
```
Windows:
```
go build -o cut.exe
```

---

## ▶️ Запуск

Передаём данные через pipe:
```
echo -e "a\tb\tc\td" | ./cut -f 1,3-4
```
Вывод:
```
a c d
```
---

## 📐 Примеры

### Вывести только 1-е и 3-е поля
```
echo "a:b:c:d" | ./cut -f 1,3 -d ":"
```
### Диапазон полей
```
echo "a,b,c,d,e" | ./cut -f 2-4 -d ","
```
### Только строки, в которых есть разделитель
```
printf "abc\n1:2:3\nxyz\n" | ./cut -f 2 -d ":" -s
```
Результат:
```
2
```
---

## 🧪 Тесты

Запуск тестов:
```
go test ./...
```

---

## 📁 Структура проекта
```
cut/
├── cut.go
├── cut_test.go
└── go.mod
```
---

## 📄 Лицензия

СДЛ (свободолюбивая)
